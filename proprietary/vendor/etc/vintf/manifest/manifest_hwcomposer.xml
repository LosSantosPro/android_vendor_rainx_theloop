<manifest version="1.0" type="device">
    <hal format="aidl">
        <name>android.hardware.graphics.composer3</name>
        <!-- Stock blob is v3; bumped to v4 for A16 FCM compat.
             AIDL negotiates down at runtime via getInterfaceVersion(). -->
        <version>4</version>
        <fqname>IComposer/default</fqname>
    </hal>
</manifest>
